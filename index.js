module.exports = {
    vow: require('./src/vow'),
    wait: require('./src/helpers').wait,
    reduce: require('./src/collections').reduce,
    map: require('./src/collections').map,
    batch: require('./src/collections').batch
}
